version: 1.0
runtime: nodejs20

build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build

run:
  command: npm run start:prod
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
