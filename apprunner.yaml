version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build

run:
  command: npm run preview -- --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
