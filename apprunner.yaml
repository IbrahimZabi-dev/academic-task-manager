version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - npm install --only=dev
      - npm run build
    build:
      - npm install --production

run:
  command: npm run start:prod
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
